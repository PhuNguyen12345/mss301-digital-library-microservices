services:
  postgres:
    image: postgres:16-alpine
    container_name: digilib-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:digilib_identity}
      POSTGRES_USER: ${POSTGRES_USER:digilib}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:digilib}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-digilib} -d ${POSTGRES_DB:-digilib_identity}"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:
